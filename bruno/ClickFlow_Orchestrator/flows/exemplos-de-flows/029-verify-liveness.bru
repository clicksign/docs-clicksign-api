meta {
  name: Verify — liveness
  type: http
  seq: 29
}

post {
  url: https://{{host}}/api/v1/flows
  body: json
  auth: inherit
}

body:json {
{
  "name": "Proposta Crédito — verify",
  "steps": [
    {
      "context": {
        "authentication": "liveness"
      },
      "type": "verify"
    }
  ]
}
}
