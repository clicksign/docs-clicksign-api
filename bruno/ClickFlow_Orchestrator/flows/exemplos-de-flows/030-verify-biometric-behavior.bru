meta {
  name: Verify — biometric_behavior
  type: http
  seq: 30
}

post {
  url: https://{{host}}/api/v1/flows
  body: json
  auth: inherit
}

body:json {
{
  "name": "Proposta Crédito — verify biometric_behavior",
  "steps": [
    {
      "context": {
        "authentication": "biometric_behavior"
      },
      "type": "verify"
    }
  ]
}
}
